To run utopia.sh properly we might need to mount a dev per time since there is high chance of id collisions, due to the fact that there are 3 filesystems in a shared file. I havent found a way to remound them elsewhere yet there is the manual way as well or with .bashcr.
